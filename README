To install:

runhaskell Setup.hs configure
runhaskell Setup.hs build
runhaskell Setup.hs install

OR, for local install (depending how your Haskell install is setup), try:

runhaskell Setup.hs configure --user
runhaskell Setup.hs build
runhaskell Setup.hs install

OR, run in interactive mode using ghci:

ghci Main.lhs

! You may need to compile the lexer and parser first if not already done!:

alex Language/Fortran/Lexer.x
happy Language/Fortran/Parser.y



